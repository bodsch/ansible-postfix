
{% if postfix_main_relay.host is defined and postfix_main_relay.host | length > 0 %}
{% if postfix_main_relay.mxlookup %}
relayhost            = {{ postfix_main_relay.host }}:{{ postfix_main_relay.port }}
{% else %}
relayhost            = [{{ postfix_main_relay.host }}]:{{ postfix_main_relay.port }}
{% endif %}
{% else %}
relayhost =
{% endif %}
{% if postfix_main_relay.domains_file and postfix_main_relay.domains_file | length > 0 %}
relay_domains        = {{ postfix_main_relay.domains_file }}
{% endif %}
{% if postfix_main_relay.recipient_maps_file and postfix_main_relay.recipient_maps_file | length > 0 %}
relay_recipient_maps = {{ postfix_main_relay.recipient_maps_file }}
{% endif %}

# relayhost            =
# relay_domains        = proxy:mysql:/opt/postfix/conf/sql/mysql_virtual_relay_domain_maps.cf
# relay_recipient_maps = proxy:mysql:/opt/postfix/conf/sql/mysql_relay_recipient_maps.cf
